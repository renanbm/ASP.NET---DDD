@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    @*using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" })) {
        @Html.AntiForgeryToken()*@

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Olá " + User.Identity.GetUserName() + "!", "Index", "Manage", null, new {title = "Gerenciar"})
        </li>
        <li>@Html.ActionLink("Sair", "SignOut", "Home")</li>
    </ul>
    //}
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Registrar", "Register", "Account", null, new {id = "registerLink"})</li>
        <li>@Html.ActionLink("Login", "Login", "Account", null, new {id = "loginLink"})</li>
    </ul>
}